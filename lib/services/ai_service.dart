class AIService {
  Future<String> sendPrompt(String prompt) async {
    // Placeholder: Later integrate with Llama
    await Future.delayed(const Duration(seconds: 1));
    return "Mock AI response for: $prompt";
  }
}

