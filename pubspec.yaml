name: aladdin_app
description: AI-based App Builder
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 🎨 UI & Animations
  lottie: ^3.1.0           # اپ ڈیٹ کیا گیا
  cupertino_icons: ^1.0.6  # اپ ڈیٹ کیا گیا
  google_fonts: ^6.1.0
  font_awesome_flutter: 10.7.0  # ✅ یہ بلکل ٹھیک ہے، اسے نہ چھیڑیں

  # 🔹 File & Storage Handling
  file_picker: ^8.0.0      # اپ ڈیٹ کیا گیا (پرانا ورژن ایرر دے سکتا ہے)
  permission_handler: ^11.3.0 # ✅ بہت ضروری اپ ڈیٹ (Kotlin ایرر ختم کرنے کے لیے)
  path_provider: ^2.1.2
  archive: ^3.4.10
  image_picker: ^1.0.7

  # 🔹 Network & APIs
  http: ^1.2.0
  url_launcher: ^6.2.5
  google_generative_ai: ^0.2.2

  # 🔹 Local Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0

  # 🔹 Authentication & Security
  local_auth: ^2.1.8

  # 🔹 Syntax Highlighting (Code Preview)
  flutter_highlight: ^0.7.0

  # ============= 🔥 FIREBASE DEPENDENCIES (Stable Versions) =============
  firebase_core: ^2.27.0   # ✅ نیا ورژن
  firebase_auth: ^4.17.8   # ✅ نیا ورژن
  google_sign_in: ^6.2.1
  
  # ✅ نیا: AdMob for Monetization
  google_mobile_ads: ^5.1.0  # ⚠️ بہت اہم: ورژن 4.0 پرانا ہے اور Kotlin کا ایرر دیتا ہے

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/fonts/

  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
          weight: 400
        - asset: assets/fonts/Poppins-Medium.ttf
          weight: 500
        - asset: assets/fonts/Poppins-Italic.ttf
          style: italic
