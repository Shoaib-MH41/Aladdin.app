name: aladdin_app
description: AI-based App Builder
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 🎨 UI & Animations
  lottie: ^2.7.0
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  font_awesome_flutter: ^10.6.0

  # 🔹 File & Storage Handling
  file_picker: ^6.1.1
  permission_handler: ^10.2.0
  path_provider: ^2.1.1
  archive: ^3.3.6
  image_picker: ^1.0.4

  # 🔹 Network & APIs
  http: ^1.1.0
  url_launcher: ^6.2.0
  google_generative_ai: ^0.2.1

  # 🔹 Local Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0

  # 🔹 Authentication & Security
  local_auth: ^2.1.7

  # 🔹 Syntax Highlighting (Code Preview)
  flutter_highlight: ^0.7.0

  # ============= 🔥 FIREBASE DEPENDENCIES =============
  # 🔥 یہ اس لیے شامل کیے کہ Aladdin AI سے بننے والی ایپس میں Login System ہو سکے
  
  # 🔥 Firebase Core - ضروری
  firebase_core: ^2.24.0
  
  # 🔥 Firebase Authentication - Login/Signup کے لیے
  firebase_auth: ^4.16.0
  
  # 🔥 (اختیاری) Google Sign In - اگر صارف Google سے Login کرنا چاہے
  google_sign_in: ^6.1.5
  
  # 🔥 (اختیاری) Facebook Login - اگر صارف Facebook سے Login کرنا چاہے
  flutter_facebook_auth: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/fonts/

  fonts:
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
          weight: 400
        - asset: assets/fonts/Poppins-Medium.ttf
          weight: 500
        - asset: assets/fonts/Poppins-Italic.ttf
          style: italic
