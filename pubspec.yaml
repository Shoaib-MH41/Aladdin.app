name: aladdin_app
description: AI-based App Builder
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  file_picker: ^6.1.1                    # ✅ صرف ایک بار
  permission_handler: ^11.0.0
  http: ^1.1.0                           # ✅ یہ نئی package
  url_launcher: ^6.2.0                   # ✅ یہ نئی package
  external_path: ^1.0.0                  # ✅ یہ نئی package
  flutter_highlight: ^0.7.0              # ✅ یہ نئی package
  google_generative_ai: ^0.2.0           # ✅ یہ نئی package
  github: ^9.0.0                         # ✅ یہ نئی package
  shared_preferences: ^2.2.2             # ✅ یہ نئی package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  # 🌟 Assets کو عارضی طور پر comment کریں
  # assets:
  #   - assets/images/
